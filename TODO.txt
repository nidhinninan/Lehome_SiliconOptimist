- build a ViT or resnet classifier to dynamically load the correct policy.
- write a skill that is targetted for working with the lerobot dataset API, Functionality, and class structure.
- figure out how to run the xvla-folding policy in the simulation.
- could also try changing a the PhysX gripper adjustment suggestion (based on research) to see if there a comparative improvement in the results.
- use isaaclab for parallel sim RLfinetuning with DPPO (maybe check viability)
- Use sim's built-in randomization (texture_randomization, light_randomization) + add image augmentation in config.
- try using a pre-trained vision backbone (need to mess around with GroupNorm vs Batch norm)
- copy over the new <scripts/utils/eval_utils.py> and <scripts/utils/evaluation.py> to the cloud.
- [x] update the split eval script to run with any garment type not just Top_short (hard coded) so that you can simply pass the garment type as an argument, like this: ./split_and_run_parallel.sh pant_long.
