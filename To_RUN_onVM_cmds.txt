# Install the Lehome workspace and directories in a new machine

```python
#create data directory
cd 
mkdir data

chmod +x ~/data/lehome_workspace/setup_lehome.sh
~/data/lehome_workspace/setup_lehome.sh
###
# probably don't need this any more (had the use principia instead of $USER in the .sh script to correctly give permissions) 
mkdir lehome_workspace
sudo chown -R principia:principia ~/data/lehome_workspace
______
# clone files manually
cd ~/data/lehome_workspace
git clone -b vastai_instance --single-branch https://nidhinninan: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@github.com/nidhinninan/Lehome_SiliconOptimist.git
______
# need this to use the rsynce between the private clone (adapted) repo and the fresh repo downloaded form the hackathon official repo
sudo apt update
sudo apt install -y rsync
______
# do this to not not loss file autogenerated through the .sh file
rsync -av \
  --exclude ".git/" \
  --exclude "lehome-challenge/.venv/" \
  --exclude "lehome-challenge/Assets/" \
  --exclude "lehome-challenge/Datasets/" \
  --exclude "lehome-challenge/third_party/" \
  ~/data/Lehome_SiliconOptimist/lehome_workspace/ \
  ~/data/lehome_workspace/
  
```

# move cloned lehome_workspace folder (private repo) to folder in the correct location on V<>
```python
mv \
  ~/data/Lehome_SiliconOptimist/lehome_workspace/ \
  ~/data/


touch .env
TERM=xterm nano ~/data/lehome_workspace/.env

set -a
source ~/data/lehome_workspace/.env
set +a  # Turn allexport back off
echo $HF_TOKEN_IDE
```
```python
#go into lehome-challenge and run uv sync
cd lehome-challenge
uv sync #(run this in the ##lehome-challlenge)

**setup rclone**
rclone config
# use this to upadte rclone and direct install may nit be the latest
curl https://rclone.org/install.sh | sudo bash

#change configs
TERM=xterm nano ~/data/lehome_workspace/configs/sweep_dino_map_registers.yaml
## chnage to root: /root/data/lehome_workspace/lehome-challenge/Datasets/example/top_long_merged
TERM=xterm nano ~/data/lehome_workspace/configs/sweep_dino_baseline.yaml
## chnage to root: /root/data/lehome_workspace/lehome-challenge/Datasets/example/top_long_merged

#change the dataset root in the yaml files
cd ../
./run_2run_dino_sweep.sh
# No Drive offload (legacy cwd-based workspace): ./run_2run_dino_sweep_no-gdrive.sh
```

# === GPU memory cleanup (run after a crashed/killed training run) ===
```python

# 1. Check what's holding GPU memory
nvidia-smi

# 2. Kill all processes still occupying /dev/nvidia* devices (catches zombie workers)
sudo fuser -k /dev/nvidia*

# 3. Also nuke any lingering Python training processes by name
pkill -9 -f lerobot_train_with_plugins || true
pkill -9 -f lerobot_train              || true

# 4. Verify GPU memory is freed (MiB used should drop to near 0)
sleep 2 && nvidia-smi --query-gpu=memory.used,memory.free --format=csv
```

# run with checkpoints saved to google drive
```python
cd /root/data/lehome_workspace   # or your synced path
chmod +x run_2run_dino_sweep.sh   # already +x in repo
ENABLE_RCLONE_CHECKPOINT_SYNC=1 ./run_2run_dino_sweep.sh
```

# DINO MAP+registers 150k on dp_top_short (uses sweep_dino_map_registers.yaml + CLI overrides; no YAML edit)
```bash
cd /root/data/lehome_workspace   # or your synced lehome_workspace path
chmod +x run_train_dino_dp_top_short_150k.sh
RESUME=false ./run_train_dino_dp_top_short_150k.sh
# resume:
RESUME=true ./run_train_dino_dp_top_short_150k.sh
# if torchcodec fails on first batch:
# EXTRA_TRAIN_ARGS="--dataset.video_backend=pyav" RESUME=false ./run_train_dino_dp_top_short_150k.sh
```

# to close a terminal session after it has immaturely shut down
```python
sudo lsof -i :8080
kill -9 <"PID-from-above-cmd">
```
# training run
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
ENABLE_RCLONE_CHECKPOINT_SYNC:1 EVAL_FREQ=1000 STEPS=3000 \
./run_train_dino_dp_top_short_150k.sh

PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True ENABLE_RCLONE_CHECKPOINT_SYNC=1 EVAL_FREQ=20000 STEPS=150000 SAVE_FREQ=30000 run_train_dino_dp_top_short_150k.sh