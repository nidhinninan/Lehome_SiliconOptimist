[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_policy_dino"
version = "0.1.0"
dependencies = [
    "lerobot==0.4.3",
    "transformers",
]

[tool.setuptools]
package-dir = {"" = "src"}
