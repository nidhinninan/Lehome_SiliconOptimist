# Copy to requirements.txt (or run build_docker_submission.sh --init-requirements).
# PyTorch + CUDA libs are pinned in Dockerfile.submission after lerobot installs.
# Keep torch/torchvision/torchaudio out of this file so the Dockerfile can enforce cu128 wheels.
numpy>=1.24,<2
opencv-python-headless>=4.8.0,<5
